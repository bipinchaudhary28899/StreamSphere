export interface Video {
    id: string;
    title: string;
    description?: string;
    thumbnail: string;
    channel: string;
    views: string;
    timestamp: string;
    category: string;
    videoUrl: string;
}
